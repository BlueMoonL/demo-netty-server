<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.mapper.GpsInfoMapper">

    <insert id="insertGpsInfo" parameterType="GPSInfo">
        INSERT INTO DEMO.GPS_INFO (
            deviceId
            , date
            , latitude
            , longitude
            , speed
        )
        VALUES (
            #{deviceId}
            , #{date}
            , #{latitude}
            , #{longitude}
            , #{speed}
        )
    </insert>

</mapper>